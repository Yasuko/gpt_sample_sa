import ResponseForm from "./reducers/ResponseForm"
import ResponseHistory from "./reducers/ResponseHistory";

/**
 * ResponseReducerオブジェクト。
 * 各リデューサーをまとめてエクスポートします。
 *
 * @property ResponseForm - ResponseFormリデューサー。
 */
export const ResponseReducer = {
    ResponseForm,
    ResponseHistory,
};
