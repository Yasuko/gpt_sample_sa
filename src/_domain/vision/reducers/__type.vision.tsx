import {
    VisionOptions,
    initialVisionOption
} from '../../../_lib/gpt/type.service'

/**
 * Visionフォームの型定義
 * VisionOptions型を使用して、Visionフォームの型を定義します。
 */
export type VisionFormType = VisionOptions

/**
 * Visionフォームの初期値
 * initialVisionOptionを使用して、Visionフォームの初期値を設定します。
 */
export const initialVisionForm: VisionFormType = initialVisionOption
