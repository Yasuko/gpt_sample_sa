import {
    WhisperOptions,
    initialWhisperOption
} from '../../../_lib/gpt/type.service'

export type WhisperScreenType = {
    showText: boolean
}

export const WhisperScreenInitialState: WhisperScreenType = {
    showText: false
}

